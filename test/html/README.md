These are samples that should eventually be moved to gists once a release process is in place, these 
will then be referenced by bl.ocks.org.

As such the samples are not complete html samples, they are samples ready for bl.ocks.org.