'use strict';

module.exports = function() {  // Injected dependencies
  function ohlc() { // Closure function
    function plot(x) {
    }

    // Rebind all methods and properties from a super class that candlestick also shares

    return plot;
  }

  return ohlc;
};